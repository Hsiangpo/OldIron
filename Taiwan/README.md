# Taiwan

## Sites

- `ieatpe` — 台北市进出口商业同业公会会员资料查询

## Commands

```bash
cd Taiwan
python -m pip install -r requirements.txt
python run.py ieatpe
```

## Delivery

This country exposes a shared-style `build_delivery_bundle` in `src/taiwan_crawler/delivery.py`.
Root `product.py` is not wired in this change.
